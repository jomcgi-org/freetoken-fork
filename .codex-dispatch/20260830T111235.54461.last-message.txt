Implemented and left uncommitted.

- Packed misses now copy directly into cache slots using one interleaved data/scale `process_vm_readv` batch.
- Page dedup runs once per install batch and ignores anonymous folded-scale pages.
- Source and destination addresses are built in coarse vectorized batches.
- Confirmed E2M1 per-shard scale folding already happens once during table loading.

Microbench, 1000 rounds:

- Before: 11,789.4 steps/sec
- After: 21,024.9 steps/sec
- Speedup: 1.78x
- fp8: 25,150.6 steps/sec
- Packed/fp8 cost: 1.20x, within the 1.3x target

Parity command produced: `12 passed, 3 skipped in 2.78s`.

Full GPU-free engine command:

```text
env PYTHONPATH=/private/tmp/freetoken-test-stubs:/Users/jomcgi/repos/FreeToken/python .venv/bin/pytest -q -rs tests/engine -k 'not gpu'
```

Summary:

```text
2 failed, 137 passed in 2.11s
```

Both failures are known macOS stub-environment failures caused by unavailable FlashInfer in untouched `test_cache_budget.py` tests. No new relevant failures were observed.

`git diff --stat`:

```text
 python/freetoken/models/qwen4_exp/ple.py | 195 ++++++++++++++++++++++++++++++-
 tests/models/qwen4_exp/test_ple_cache.py | 127 ++++++++++++++++++++
 2 files changed, 321 insertions(+), 1 deletion(-)
```

Changed [ple.py](/Users/jomcgi/repos/FreeToken/python/freetoken/models/qwen4_exp/ple.py:1249) and [test_ple_cache.py](/Users/jomcgi/repos/FreeToken/tests/models/qwen4_exp/test_ple_cache.py:112). Ruff and `git diff --check` pass.

Open question: the real cached-backend node-4 serving measurement remains to be run because this host is macOS without CUDA or Linux `process_vm_readv`. No commit or push was performed.