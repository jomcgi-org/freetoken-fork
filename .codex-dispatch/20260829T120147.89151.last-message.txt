Implemented the PLE staging fast path and left the diff uncommitted per repository guardrails.

Files changed:

- [ple.py](/Users/jomcgi/repos/FreeToken/python/freetoken/models/qwen4_exp/ple.py): vectorized hash, fixed workspaces, vectorized page dedup, batched `process_vm_readv` row copying, retained reference paths.
- [model.py](/Users/jomcgi/repos/FreeToken/python/freetoken/models/qwen4_exp/model.py): pinned context buffers, batched history assembly, event dedup, aggregate staging timing.
- [scheduler.py](/Users/jomcgi/repos/FreeToken/python/freetoken/scheduler/scheduler.py): `ple_staging_us` in the PLE stats line.
- [test_weight.py](/Users/jomcgi/repos/FreeToken/tests/models/qwen4_exp/test_weight.py): 1,000-step parity benchmark and replay bookkeeping coverage.

Validation:

```text
81 passed, 12 skipped, 1 warning in 4.05s
```

Micro-benchmark on this Apple CPU:

```text
before=5470.4 steps/sec, after=8425.5 steps/sec
```

That is about a 54% throughput improvement. Hash IDs, compact IDs, and staged bytes matched the retained scalar implementation across all 1,000 steps.

Deviations:

- The target L4 box was unavailable, so the 10% end-to-end target remains to be confirmed there.
- macOS exercised the `memmove` fallback. Linux serving will use the single-call `process_vm_readv` path for bs=1.
- CUDA and external HF-reference tests account for the skips.
- No commit or push was performed. The existing `.codex-dispatch/` files were untouched.